spring:
  profiles:
    active: dev # dev | prod

management:
  endpoint:
    health:
      probes:
        enabled: true   # readiness/liveness 사용시
  metrics:
    tags:
      application: app

server:
  port: 8080 # app / 8081 # batch
