spring:
  profiles:
    active: prod # dev | prod
  config:
    import: optional:file:.env[.properties]

monew:
  storage:
    s3:
      access-key: ${AWS_ACCESS_KEY}
      secret-key: ${AWS_SECRET_KEY}
      region: ${AWS_S3_REGION}
      bucket: ${AWS_S3_BUCKET}
      expiration: ${AWS_S3_PRESIGNED_URL_EXPIRATION:600}

management:
  endpoint:
    health:
      probes:
        enabled: true   # readiness/liveness 사용시
  metrics:
    tags:
      application: app
    export:
      cloudwatch:
        enabled: ${CLOUDWATCH_METRICS_ENABLED:false}
        namespace: ${CLOUDWATCH_NAMESPACE:monew/app}
        region: ${AWS_CLOUDWATCH_REGION:${AWS_S3_REGION}}
        step: 1m

server:
  port: 8080 # app / 8081 # batch
  servlet:
    session:
      cookie:
        same-site: none
        http-only: true
        secure: true
